# python code/classification/train.py -c classification/config/E-ResNet18.txt

# Output settings
exp_name = E-ResNet18
output_dir = classification/output
checkpoint_dir = classification/checkpoints/debugging

# General settings
device = cpu
dtype = float32
seed = 12
#load_checkpoint = classification/output/E-ResNet18.pth

# General training hyperparameters
num_epochs = 300
batch_size = 16
lr = 5e-2
weight_decay = 5e-4
optimizer = Adam


# :LR scheduling hyperparameters
# choices=[None, "cosine", "step", "exponential", "plateau", "cos_sim"]
lr_type = cos_sim
lr_epochs = 10
lr_factor = 0.95


# General validation/testing hyperparameters
batch_size_test = 128

# Model selection
num_layers = 18
embedding_dim = 16
encoder_manifold = euclidean
decoder_manifold = euclidean

# Dataset settings
dataset = Tiny-ImageNet # CIFAR-10 or Tiny-ImageNet or CIFAR-100


# How many samples we use for debugging
train_size = 32